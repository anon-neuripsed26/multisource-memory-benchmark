"""Paper-reproduction entry points.

Each ``table_N_*.py`` module rebuilds one table from frozen artifacts and
writes a CSV + Markdown pair under :mod:`paper_artifacts.output`.
``reproduce_paper.py`` is the CLI orchestrator. See ``README.md`` and
``MANIFEST.md`` for the full inventory.
"""
